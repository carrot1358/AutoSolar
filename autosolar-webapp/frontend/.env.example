# ── Backend API URL ───────────────────────────────────────────────────────────
# Used by the frontend to connect to the backend API and WebSocket server.
# In production, set this to your deployed backend URL.
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000
